name: amber_render_module
version: 0.1.4

authors:
  - Damian Hamill <damianham@gmail.com>

license: MIT

description: |
  Render templates and partials from the same folder as the calling controller/mailer/view so all the artifacts for a feature code can be placed in the same folder
